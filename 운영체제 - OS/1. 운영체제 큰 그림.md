!\[](https://velog.velcdn.com/images/kmwook/post/e287780b-b1d2-43c4-9015-c30b95665238/image.png)



---

> ## 운영체제 구성요소



\### 커널(Kernel)



운영체제의 핵심 서비스를 담당하는 소프트웨어로 하드웨어의 자원을 필요에 따라 프로세스에 나눠주고 이후 설명할 시스템 콜 등을 수행한다.



!\[](https://velog.velcdn.com/images/kmwook/post/cb351f2a-9e15-4543-83f9-a068cdee0b19/image.png)



운영체제가 설치된 모든 기에는 커널이 존재한다. 어떤 커널을 사용하는지에 따라 프로그램이 하드웨어를 이용하는 양상이 달라진다.



사용자는 운영체제가 제공하는 사용자 인터페이스(UI; User Interface)를 사용해 컴퓨터(커널)와 상호작용할 수 있다.



\### 이중 모드(Dual Mode)



응용 프로그램이 하드웨어 자원에 직접 접근할 수 있다면 자원 관리에 질서가 없어지고 컴퓨터 전체에 큰 악영향을 끼칠 수 있다. 이를 방지하기 위해 운영체제는 이중 모드(dual mode)로 자원을 보호한다.



\- \*\*사용자 모드(user mode)\*\*

운영체제 서비스를 제공받을 수 없는 실행 모드.

즉, 커널 영역의 코드를 실행할 수 없는 모드다. 사용자 모드로 실행 중인 CPU는 입출력 명령어와 같이 하드웨어 자원에 접근하는 명령어를 실행할 수 없다.



\- \*\*커널 모드(kernel mode)\*\*

운영체제 서비스를 제공받을 수 있는 실행 모드.

즉, 커널 영역의 코드를 실행할 수 있는 모드다. 커널 모드로 실행 중인 CPU는 자원에 접근하는 명령어를 비롯한 모든 명령어를 실행할 수 있다. 운영체제는 커널 모드로 실행된다.



\### 시스템 콜(System Call)



사용자 모드로 실행되는 프로그램이 운영체제 서비스를 제공받기 위해 보내는 요청을 시스템 콜, 또는 시스템 호출이라고 한다.

시스템 콜은 일종의 \*\*소프트웨어 인터럽트(interrupt)\*\*다.



\- \*\*시스템 콜 처리 과정\*\*

```

1\. 시스템 호출 발생 -> 커널 모드로 전환

2\. 운영체제 내의 코드 실행 -> 하드웨어 접근

3\. 하드웨어 접근 종료 -> 사용자 모드로 복귀

```

---



> ## 운영체제 핵심 서비스



\### 프로세스 관리



실행 중인 프로그램을 프로세스(process)라고 한다. 컴퓨터를 사용하는 동안 메모리 안에서는 새로운 프로세스들이 생성되고, 사용되지 않는 프로세스는 메모리에서 삭제된다. 일반적으로 하나의 CPU는 한 번에 하나의 프로세스만 실행 가능하기 때문에 CPU는 프로세스들을 조금씩 번갈아 가며 실행한다. 자세한 내용은 다음 게시글에서 공부할 것이다.



\### 자원 접근 및 할당



모든 프로세스는 실행을 위해 자원이 필요하다. 운영체제는 프로세스들이 사용할 자원에 접근하고 조작함으로써 프로세스에 필요한 자원을 할당해 준다.



\### 파일 시스템 관리



컴퓨터에 존재하는 파일을 열고 생성하고 삭제하는 시스템을 파일 시스템(file system)이라고 한다. 운영체제는 파일 시스템에 따라서 파일을 관리한다.



---

<span style="color:gray"><참고자료>

강민철, 혼자 공부하는 컴퓨터구조+운영체제, 한빛미디어 </span>

